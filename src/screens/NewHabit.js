import { View, Text } from 'react-native';

export default function NewHabitScreen({ navigation }) {
  return (
    <View style={{ flex: 1}}>
      <Text>Welcome to the New Habit Screen!</Text>
    </View>
  );
}